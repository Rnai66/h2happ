import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.h2h.surin',
    appName: 'H2H Surin',
    webDir: 'dist',
    server: {
        androidScheme: 'https'
    }
};

export default config;
